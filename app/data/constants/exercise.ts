export enum ExerciseSetType {
  WarmUp = "warmup",
  Normal = "normal",
  DropSet = "dropset",
  Failure = "failure",
}

export enum ExerciseSource {
  Public = "public",
  Private = "private",
}

export enum ExerciseVolumeType {
  Reps = "Reps",
  Time = "Time",
}
